Analyzes BP4D+ physiological signals (BP, EDA, pulse, respiration) to classify pain/no-pain states using machine learning. Extracts statistical features from raw signals, trains an SVM classifier with RBF kernel, and validates using 5-fold cross-validation. Includes automated dataset extraction, visualization of results through confusion matrices and ROC curves, and exports trained model for deployment.
